import React from 'react'
import { useParams } from "react-router-dom";

export default function List () {
    // let { artist } = useParams();

    return (
        <div>Asasasasasasa {artist}</div>
    )
}